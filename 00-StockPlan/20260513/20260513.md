下面是下一輪 **24H 長時間任務 Prompt**。依 P24 結論，MonthlyRevenue production migration 已執行完成，且 `productionMigrationApplied:true` 已如實記錄；下一步應做 **P25 Post-Migration Observability + Active Scoring Smoke**，確認 production DB migration 後，active scoring、query gate、snapshot capture、MonthlyRevenue availability 都能正常運作，且不造成 scoring / bucket / reason/signal/factor 的非預期變化。P24 已完成 backup、migration、backfill、post-migration validation、rollback readiness，final classification 為 `P24_PRODUCTION_MIGRATION_EXECUTION_COMPLETE`。

---

# Next 24H Prompt — P25 Post-Migration Observability + Active Scoring Smoke

```text
你是 Stock Prediction System 的 Senior Post-Migration Observability / Active Scoring Smoke Agent。

任務名稱：
P25-HARDRESET — Post-Migration Observability + Active Scoring Smoke after MonthlyRevenue releaseDate Migration

日期：
2026-05-12

工作目錄：
/Users/kelvin/Kelvin-WorkSpace/Stock-Prediction-System

目前狀態：
P24-HARDRESET 已完成：
- Final Classification: P24_PRODUCTION_MIGRATION_EXECUTION_COMPLETE
- Commits:
  - 33e5958 P24-HARDRESET: MonthlyRevenue production migration execution gate
  - 5df2036 P24 final report
- Token verified:
  P23_APPROVE_PRODUCTION_MIGRATION_EXECUTION_ONLY
- Preflight = 27/27 PASS
- Backup gate = PASS
  - dev.db backed up
  - sha256 checksum recorded
  - MonthlyRevenue rows before migration = 2,143
- Migration gate = PASS
  - 3 columns added:
    - releaseDate
    - releaseDateSource
    - releaseDateConfidence
  - 0 columns dropped
- Backfill gate = PASS
  - 2,143 MonthlyRevenue rows backfilled
  - releaseDate = 10th day of following month
  - releaseDateSource = INFERRED_NEXT_MONTH_10TH
  - explicit releaseDates overwritten = 0
- Post-migration validation = PASS
  - 13/13 MON items PASS
  - query gate smoke PASS
  - no leakage detected
- Rollback readiness = PASS
  - 8 rollback triggers
  - 10 restore steps
  - backup intact
- Tests:
  - P24 unit tests = 108/108 PASS
  - broader suite = 1989 PASS
- Artifact validation = 35/35 PASS
- Forbidden claims scan = CLEAN
- productionMigrationApplied = true
- Frozen corpora unchanged:
  - simulation_snapshot_corpus.jsonl = 60
  - p0hardreset_historical_replay_corpus.jsonl = 4500
  - p1baseline_historical_replay_corpus.jsonl = 9900
  - p3active_scoring_historical_replay_corpus.jsonl = 4500
  - p19active_scoring_pit_replay_corpus.jsonl = 4500
- scoring formula / alphaScore / recommendationBucket unchanged

本輪目標：
在 MonthlyRevenue releaseDate production migration 完成後，執行 post-migration observability 與 active scoring smoke validation。
本輪不是模型調參。
本輪不是 corpus 大規模 regeneration。
本輪不是投資績效評估。
本輪目標是確認 production migration 後：
1. MonthlyRevenue schema 欄位仍存在
2. backfill releaseDate 分布正確
3. query gate releaseDate <= asOfDate 正常
4. RuleBasedStockAnalyzer 可正常執行
5. FundamentalResearchService 可正常執行
6. ActiveScoringSnapshot 可正常 capture
7. reason / signal / factor snapshot 不含未公告 MonthlyRevenue
8. scoring formula / alphaScore / recommendationBucket 未被修改
9. frozen corpora unchanged
10. production migration 後系統可進入 P26 post-migration replay comparison

本輪不得：
- 再次執行 production migration
- drop / alter production DB schema
- 修改 MonthlyRevenue releaseDate / source / confidence 欄位值
- 覆蓋 P0 / P1 / P3 / P4 / P19 corpus
- 修改 simulation_snapshot_corpus.jsonl
- 修改 ManualReview* 模組
- 修改 scoring formula
- 修改 alphaScore / recommendationBucket 計算邏輯
- 根據 realized return 調整 releaseDate / score / bucket
- 呼叫外部 API / LLM
- 自動交易
- 宣稱 ROI / win-rate / alpha / edge / profit
- 輸出 buy/sell/recommendation/outperform/guaranteed
- 將 migration 成功解讀為模型有效

本輪可以：
- 讀取 production/dev DB schema 與 MonthlyRevenue rows
- 執行 post-migration smoke scripts
- 執行 limited active scoring smoke
- 建立 observability artifacts
- 建立 post-migration snapshot validation
- 新增 tests
- 新增 final report

============================================================
PART A — Pre-flight Gate
============================================================

A.1 確認 P24 artifacts 存在：
- outputs/online_validation/p24production_migration_execution_final_report.md
- outputs/online_validation/p24production_backup_gate.json
- outputs/online_validation/p24production_migration_gate.json
- outputs/online_validation/p24production_backfill_gate.json
- outputs/online_validation/p24production_post_migration_validation.json
- outputs/online_validation/p24production_rollback_readiness.json

A.2 驗證 P24 結論：
- Final Classification = P24_PRODUCTION_MIGRATION_EXECUTION_COMPLETE
- backup gate = PASS
- migration gate = PASS
- backfill gate = PASS
- post-migration validation = PASS
- rollback readiness = PASS
- productionMigrationApplied = true
- rowsBackfilled = 2143
- explicit releaseDates overwritten = 0
- forbidden claims = CLEAN

A.3 DB schema checks：
確認 MonthlyRevenue schema 包含：
- releaseDate
- releaseDateSource
- releaseDateConfidence

A.4 Frozen corpus checks：
- simulation_snapshot_corpus.jsonl = 60 lines
- p0hardreset_historical_replay_corpus.jsonl = 4500 lines
- p1baseline_historical_replay_corpus.jsonl = 9900 lines
- p3active_scoring_historical_replay_corpus.jsonl = 4500 lines
- p19active_scoring_pit_replay_corpus.jsonl = 4500 lines

輸出：
- outputs/online_validation/p25post_migration_observability_preflight.json
- outputs/online_validation/p25post_migration_observability_preflight.md

若 artifacts 不完整：
- outputs/online_validation/p25post_migration_observability_escalation_report.md
- Final Classification = P25_POST_MIGRATION_BLOCKED_BY_ARTIFACTS

============================================================
PART B — Post-Migration Observability Utility
============================================================

新增：
src/lib/onlineValidation/P25PostMigrationObservabilityUtils.ts

必須 export：
- validateMonthlyRevenueSchemaPostMigration(schema)
- summarizeMonthlyRevenueReleaseDateDistribution(rows)
- validateReleaseDateBackfillDistribution(rows)
- validateMonthlyRevenueQueryGateSmoke(rows, asOfDates)
- validateNoUnreleasedMonthlyRevenueInSnapshot(snapshot, asOfDate)
- summarizeActiveScoringSmokeResult(results)
- compareSmokeSnapshotToExpectedContract(snapshot, contract)
- buildPostMigrationObservabilitySummary(inputs)
- scanForbiddenClaims(text)

Hard rules：
1. releaseDate 欄位必須存在
2. releaseDateSource 欄位必須存在
3. releaseDateConfidence 欄位必須存在
4. inferred releaseDate rows 必須標記 INFERRED_NEXT_MONTH_10TH
5. releaseDateConfidence 必須可觀測
6. query gate 必須 obey releaseDate <= asOfDate
7. snapshot 不得包含 unreleased MonthlyRevenue
8. 不得用 outcome / returnPct / realizedReturnClass 參與 smoke 判斷
9. 不得修改 corpus
10. 不得修改 scoring formula

============================================================
PART C — MonthlyRevenue Distribution Audit
============================================================

新增 script：
scripts/run-p25-monthly-revenue-distribution-audit.js

輸出：
- outputs/online_validation/p25monthly_revenue_distribution_audit.json
- outputs/online_validation/p25monthly_revenue_distribution_audit.md

必須統計：
- total MonthlyRevenue rows
- rows with releaseDate
- rows without releaseDate
- releaseDateSource distribution
- releaseDateConfidence distribution
- invalid releaseDate count
- duplicate stockId/year/month count
- min releaseDate
- max releaseDate
- sample inferred rows
- sample rows by month boundary
- production DB write status = false for this phase

驗收：
- total rows >= 2143
- rows with releaseDate >= 2143
- releaseDateSource includes INFERRED_NEXT_MONTH_10TH
- invalid releaseDate count = 0
- no production write in this phase

============================================================
PART D — MonthlyRevenue Query Gate Smoke
============================================================

新增 script：
scripts/run-p25-monthly-revenue-query-gate-smoke.js

輸出：
- outputs/online_validation/p25monthly_revenue_query_gate_smoke.json
- outputs/online_validation/p25monthly_revenue_query_gate_smoke.md

Smoke cases：
1. pick sample January 2024 MonthlyRevenue row:
   - asOfDate one day before releaseDate => unavailable
   - asOfDate equal releaseDate => available
   - asOfDate one day after releaseDate => available

2. pick sample December 2024 MonthlyRevenue row:
   - asOfDate one day before releaseDate => unavailable
   - asOfDate equal releaseDate => available

3. query with allowInferredReleaseDate=false:
   - inferred rows unavailable unless authoritative releaseDate exists

4. invalid / missing releaseDate rows:
   - unavailable

5. MonthlyRevenueAvailability helper:
   - must match DB-level query gate behavior

驗收：
- all smoke cases PASS
- no outcome fields used
- no scoring formula changed

============================================================
PART E — Active Scoring Smoke
============================================================

新增 script：
scripts/run-p25-active-scoring-smoke-after-migration.js

輸入：
- MonthlyRevenueAvailability helper
- RuleBasedStockAnalyzer
- FundamentalResearchService if callable
- ActiveScoringSnapshotBuilder
- small deterministic symbol/date sample

輸出：
- outputs/online_validation/p25active_scoring_smoke_after_migration.json
- outputs/online_validation/p25active_scoring_smoke_after_migration.md

抽樣規則：
- deterministic sample
- 至少 5 symbols
- 至少 5 asOfDates
- 至少 3 horizons if applicable
- 不得使用 Math.random

必須驗證：
- active scoring returns result
- scoringCompletenessStatus exists
- researchBucket exists
- scoreSnapshot exists
- reasonSnapshot / signalSnapshot / factorSnapshot 可觀測
- MonthlyRevenue unavailable before releaseDate is excluded
- MonthlyRevenue available after releaseDate can be observed if source data exists
- snapshot 不含 outcomePrice / returnPct / realizedReturnClass
- no production corpus modified

若 smoke 無法呼叫某 service：
- 不得中止整輪
- 標記 serviceCallable=false
- 輸出 limitation
- classification 可為 PARTIAL

============================================================
PART F — Post-Migration Contract Validation
============================================================

新增 script：
scripts/validate-p25-post-migration-contract.js

輸入：
- P12 PIT feature contract
- P17 MonthlyRevenueAvailability helper
- P24 post-migration artifacts
- P25 distribution / query gate / active scoring smoke artifacts

輸出：
- outputs/online_validation/p25post_migration_contract_validation.json
- outputs/online_validation/p25post_migration_contract_validation.md

必須驗證：
- P12 MonthlyRevenue PIT requirement satisfied
- P17 query gate patch still present
- P24 migration result still valid
- P25 distribution audit PASS
- P25 query gate smoke PASS
- active scoring smoke PASS or PARTIAL with documented limitation
- no forbidden snapshot fields
- no corpus modification
- no scoring formula modification

============================================================
PART G — Tests
============================================================

新增測試：
src/lib/onlineValidation/__tests__/p25post_migration_observability_utils.test.ts

至少涵蓋：
- validateMonthlyRevenueSchemaPostMigration catches missing releaseDate
- validateMonthlyRevenueSchemaPostMigration catches missing source/confidence
- summarizeMonthlyRevenueReleaseDateDistribution deterministic
- validateReleaseDateBackfillDistribution rejects invalid date
- validateMonthlyRevenueQueryGateSmoke before/equal/after releaseDate
- validateNoUnreleasedMonthlyRevenueInSnapshot catches unreleased MonthlyRevenue
- validateNoUnreleasedMonthlyRevenueInSnapshot catches outcomePrice / returnPct / realizedReturnClass
- summarizeActiveScoringSmokeResult deterministic
- scanForbiddenClaims catches ROI / alpha / edge / profit / outperform / buy / sell / guaranteed
- 不使用 Math.random
- 不修改 P0/P1/P3/P19 corpus

執行：
npx jest src/lib/onlineValidation/__tests__/p25post_migration_observability_utils.test.ts --no-coverage
npx jest src/lib/onlineValidation/__tests__ --no-coverage
npx jest src/lib/data/__tests__ --no-coverage
npx tsc --noEmit

若 tsc 有既有非本輪錯誤：
- 必須列出並確認不來自 P25 files
- 不得隱瞞

============================================================
PART H — Forbidden Claims Scan
============================================================

掃描：
grep -RniE "ROI|win-rate|win rate|alpha|edge|profit|outperform|beat|buy|sell|guaranteed|investment recommendation" \
  outputs/online_validation/p25* src/lib/onlineValidation/P25PostMigrationObservabilityUtils.ts scripts/run-p25-*.js scripts/validate-p25-*.js || true

允許：
- disclaimer context
- forbidden claim scanner test data
- 欄位名稱 alphaScore

若有非 disclaimer 語境，必須修正。

============================================================
PART I — Artifact Validation
============================================================

JSON parse：
for f in outputs/online_validation/p25*.json; do
  node -e "JSON.parse(require('fs').readFileSync('$f','utf8')); console.log('$f OK')"
done

Frozen line count check：
node -e "
const fs = require('fs');
const expected = {
  'outputs/online_validation/simulation_snapshot_corpus.jsonl': 60,
  'outputs/online_validation/p0hardreset_historical_replay_corpus.jsonl': 4500,
  'outputs/online_validation/p1baseline_historical_replay_corpus.jsonl': 9900,
  'outputs/online_validation/p3active_scoring_historical_replay_corpus.jsonl': 4500,
  'outputs/online_validation/p19active_scoring_pit_replay_corpus.jsonl': 4500
};
for (const [path, count] of Object.entries(expected)) {
  const lines = fs.readFileSync(path, 'utf8').trim().split('\n').length;
  console.log(path, lines);
  if (lines !== count) throw new Error(path + ' changed: ' + lines);
}
console.log('all frozen line counts OK');
"

P25 artifact structure check：
node -e "
const fs = require('fs');
const dist = JSON.parse(fs.readFileSync('outputs/online_validation/p25monthly_revenue_distribution_audit.json','utf8'));
const gate = JSON.parse(fs.readFileSync('outputs/online_validation/p25monthly_revenue_query_gate_smoke.json','utf8'));
const smoke = JSON.parse(fs.readFileSync('outputs/online_validation/p25active_scoring_smoke_after_migration.json','utf8'));
const contract = JSON.parse(fs.readFileSync('outputs/online_validation/p25post_migration_contract_validation.json','utf8'));
if (!dist.validationStatus) throw new Error('missing distribution validationStatus');
if (!gate.validationStatus) throw new Error('missing query gate validationStatus');
if (!smoke.smokeStatus) throw new Error('missing active scoring smokeStatus');
if (!contract.validationStatus) throw new Error('missing contract validationStatus');
console.log('P25 distribution:', dist.validationStatus);
console.log('P25 query gate:', gate.validationStatus);
console.log('P25 smoke:', smoke.smokeStatus);
console.log('P25 contract:', contract.validationStatus);
"

============================================================
PART J — Commit
============================================================

若全部 PASS，commit：

P25-HARDRESET: Post-migration observability and active scoring smoke

- New: post-migration observability utilities
- New: MonthlyRevenue distribution audit
- New: MonthlyRevenue query gate smoke
- New: active scoring smoke after migration
- New: post-migration contract validation
- Frozen: P0/P1/P3/P19/frozen corpus unchanged
- Frozen: scoring formula / alphaScore / recommendationBucket unchanged
- No production migration re-run
- No ROI / alpha / edge / win-rate claims

============================================================
PART K — Final Report
============================================================

新增：
outputs/online_validation/p25post_migration_observability_final_report.md

格式：

P25-HARDRESET Final Report

1. 本輪目標
2. P24 production migration recap
3. Pre-flight 結論
4. MonthlyRevenue distribution audit 結果
5. Query gate smoke 結果
6. Active scoring smoke 結果
7. Post-migration contract validation
8. Production DB write summary
9. Corpus freeze verification
10. Scoring formula unchanged verification
11. 修改 / 新增檔案清單
12. 測試結果 / regression 結果
13. TypeScript validation 結果
14. Forbidden claims scan 結果
15. Artifact validation 結果
16. 對 CEO 兩大主軸的貢獻
    - 主軸 A：直接貢獻，驗證 production migration 後 MonthlyRevenue PIT gate 可被 active scoring 正常使用
    - 主軸 B：直接貢獻，為後續 post-migration replay comparison 建立 smoke evidence
17. 風險與不確定點
18. 下一輪建議
    - 若 PASS：P26 Post-Migration Active Scoring Replay Comparison
    - 若 query gate fail：P26 Query Gate Repair
    - 若 active scoring smoke partial：P26 Active Scoring Smoke Repair
    - 若 contract validation fail：P26 PIT Contract Repair
19. Final Classification

Final Classification 七選一：
1. P25_POST_MIGRATION_OBSERVABILITY_COMPLETE
2. P25_ACTIVE_SCORING_SMOKE_PARTIAL
3. P25_QUERY_GATE_SMOKE_FAILED
4. P25_DISTRIBUTION_AUDIT_FAILED
5. P25_CONTRACT_VALIDATION_FAILED
6. P25_POST_MIGRATION_BLOCKED_BY_ARTIFACTS
7. P25_FAILED_TESTS
```

---

## CTO Agent 10 行內摘要

1. P24-HARDRESET 已完成，commits `33e5958` + `5df2036`。
2. Final Classification 為 `P24_PRODUCTION_MIGRATION_EXECUTION_COMPLETE`。
3. Production migration token 已驗證，preflight 27/27 PASS。
4. Backup gate PASS，dev.db 已備份並記錄 sha256，MonthlyRevenue before count = 2,143。
5. Migration gate PASS，新增 releaseDate / releaseDateSource / releaseDateConfidence，0 欄位 dropped。
6. Backfill gate PASS，2,143 rows 設為 `INFERRED_NEXT_MONTH_10TH`，0 explicit releaseDate overwritten。
7. Post-migration validation PASS，13/13 MON items，query gate smoke PASS，no leakage。
8. Rollback readiness PASS，8 triggers、10 restore steps，backup intact。
9. Frozen corpus 60/4500/9900/4500/4500 unchanged，scoring formula / alphaScore / recommendationBucket unchanged。
10. 下一輪建議 P25：Post-Migration Observability + Active Scoring Smoke，驗證 migration 後系統行為。
