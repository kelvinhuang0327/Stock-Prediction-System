# Stock Prediction System — P5-HARDRESET 工程交接報告

## 1. 本輪目標

本輪目標為完成：

**P5-HARDRESET — Manual Walkthrough of Calibration Cases**

目的不是調整模型，也不是宣稱績效，而是針對 P4 產出的 deterministic walkthrough cases 做人工式工程審查，確認 active scoring output 的可解釋性、score/bucket 一致性、signal/reason 合理性，並整理下一輪修補優先序。

---

## 2. 已完成事項

### 2.1 完成 P5-HARDRESET 全部 A–I 階段

| Part   |   狀態 | 說明                        |
| ------ | ---: | ------------------------- |
| PART A | PASS | Pre-flight audit 完成       |
| PART B | PASS | Review rubric contract 完成 |
| PART C | PASS | Walkthrough review 完成     |
| PART D | PASS | Repair backlog 完成         |
| PART E | PASS | 測試完成                      |
| PART F | PASS | Forbidden claims scan 完成  |
| PART G | PASS | Artifact validation 完成    |
| PART H | PASS | Commit 完成                 |
| PART I | PASS | Final report 完成           |

### 2.2 完成 P5 walkthrough review

本輪針對 **58 個 P4 calibration walkthrough cases** 完成 manual-style deterministic review。

主要結果：

* **58/58 cases explainability = COMPLETE**
* **53/58 score ↔ bucket = CONSISTENT**
* **5/58 score ↔ bucket = INCONSISTENT**
* **34/58 signal/reason = CONSISTENT**
* **24/58 signal/reason = GENERIC**
* **29/58 cases = READY_FOR_NEXT_AUDIT**

### 2.3 完成 repair backlog 分類

本輪已將後續修補方向分出優先順序：

| 優先層級 | 問題                                | 建議方向                                    |
| ---- | --------------------------------- | --------------------------------------- |
| P0   | 5 cases score/bucket inconsistent | Bucket Schema Repair                    |
| P1   | 24 cases signal/reason generic    | Signal / Reason Snapshot Quality Repair |
| P2   | 持續觀測與下一輪 audit                    | READY_FOR_NEXT_AUDIT                    |

### 2.4 Commit 完成

已完成 commit：

```text
4c7cab7 P5-HARDRESET PART I: final report (p5walkthrough_final_report.md)
```

---

## 3. 修改或產出的檔案

### 3.1 最終報告

```text
outputs/online_validation/p5walkthrough_final_report.md
```

### 3.2 本輪應已產出的主要 P5 artifacts

依 P5 任務規格與結果，本輪應包含：

```text
outputs/online_validation/p5walkthrough_preflight_audit.json
outputs/online_validation/p5walkthrough_preflight_audit.md
outputs/online_validation/p5walkthrough_review.json
outputs/online_validation/p5walkthrough_review.md
outputs/online_validation/p5walkthrough_repair_backlog.json
outputs/online_validation/p5walkthrough_repair_backlog.md
outputs/online_validation/p5walkthrough_final_report.md
```

### 3.3 本輪應已新增 / 修改的程式與測試

```text
src/lib/onlineValidation/P5WalkthroughReviewUtils.ts
src/lib/onlineValidation/__tests__/p5walkthrough_review_utils.test.ts
scripts/run-p5-walkthrough-review.js
scripts/build-p5-walkthrough-repair-backlog.js
```

---

## 4. 驗證結果 / 測試結果

### 4.1 測試結果

| 驗證項目                     |         結果 |
| ------------------------ | ---------: |
| P5 unit tests            |       PASS |
| 測試數                      | 68/68 PASS |
| Forbidden claims scan    |       PASS |
| Frozen corpus validation |       PASS |
| P5 artifact validation   |       PASS |

### 4.2 Corpus freeze 驗證

本輪確認 4 個 corpus 檔案保持 frozen / unchanged：

| Corpus                                            |        狀態 |
| ------------------------------------------------- | --------: |
| `simulation_snapshot_corpus.jsonl`                | unchanged |
| `p0hardreset_historical_replay_corpus.jsonl`      | unchanged |
| `p1baseline_historical_replay_corpus.jsonl`       | unchanged |
| `p3active_scoring_historical_replay_corpus.jsonl` | unchanged |

### 4.3 安全限制驗證

本輪未做：

* 未修改 production DB
* 未修改 scoring formula
* 未修改 alphaScore / recommendationBucket 計算邏輯
* 未修改 P0 / P1 / P3 / P4 corpus
* 未修改 ManualReview* 模組
* 未呼叫外部 API / LLM
* 未自動交易
* 未宣稱 ROI / win-rate / alpha / edge / profit

---

## 5. 目前結論

本輪結論為：

```text
P5_WALKTHROUGH_REVIEW_COMPLETE
+
P5_REQUIRES_BUCKET_SCHEMA_REPAIR
```

P5 已完成 manual-style engineering review，並確認目前 active scoring explainability 整體是可讀的：

* **Explainability 完整度良好**：58/58 COMPLETE。
* **Score/bucket 大多一致**：53/58 CONSISTENT。
* **存在明確 P0 repair target**：5 筆 score/bucket inconsistent。
* **Signal/reason 品質仍需改善**：24/58 GENERIC，比例約 41%。

因此下一輪不應急著擴 corpus，也不應做 UI，而應先針對 **5 個 bucket schema inconsistent cases** 做診斷與修補設計。

---

## 6. 尚未完成事項

### 6.1 尚未修補 bucket schema

目前只定位出 5 個 inconsistent cases，尚未進一步分析：

* 是 bucket mapping 問題
* 還是 score threshold 問題
* 還是中英文 bucket normalization 問題
* 還是 factor-to-bucket aggregation 問題

### 6.2 尚未改善 generic signal/reason

24/58 cases 的 signal/reason 被標記為 GENERIC。
目前尚未判斷是：

* reason template 太籠統
* signal snapshot 缺少關鍵 factor
* factor explanation 未被完整 capture
* scoring engine 本身輸出不足

### 6.3 尚未做模型或 scoring 修正

本輪刻意沒有修 scoring，這是正確的。
下一輪也應先做 diagnosis，不應直接改公式。

### 6.4 尚未進入更大規模 corpus expansion

目前仍維持：

* P3 active-scoring corpus：4500 lines
* 25 symbols
* 60 asOfDates
* 3 horizons

後續若 bucket/schema 修補完成，才適合擴大到更多 symbols / dates。

---

## 7. 風險與不確定點

### 7.1 Bucket schema inconsistency 可能影響 calibration audit 判讀

5/58 cases inconsistent 雖然比例不高，但這是 **P0 repair target**。
如果 bucket schema 不穩定，後續 bucket-level calibration audit 可能被誤導。

### 7.2 Signal/reason generic 比例偏高

24/58 generic，約 41%。
這不代表 scoring 無效，但代表 explainability layer 還不夠精準，可能影響人工 review 與後續 CTO/CEO 判斷。

### 7.3 目前仍不能宣稱模型有效

雖然 P0–P5 已建立完整 observability loop，但仍不能宣稱：

* 有 alpha
* 有 edge
* 有 ROI
* 有 win-rate
* 優於 baseline
* 可做投資建議

目前只能說：**系統已具備進一步診斷 scoring/bucket/schema 的資料基礎。**

### 7.4 不宜恢復 ManualReview UI track

P15–P17 的 ManualReview UI / workflow 軌道仍應保持凍結。
目前應優先處理 scoring schema 與 explainability，而不是 UI。

---

## 8. 建議今天優先處理的方向

### 第一優先：P6 Bucket Schema Repair

原因：

* P5 classification 已明確指向 `P5_REQUIRES_BUCKET_SCHEMA_REPAIR`
* 5 個 inconsistent cases 是 P0 repair target
* bucket schema 是後續 calibration audit、walkthrough、readiness decision 的核心欄位

建議 P6 只做 diagnosis + schema repair proposal，不直接大改 scoring formula。

### 第二優先：Signal / Reason Snapshot Quality Repair

原因：

* 24/58 cases signal/reason generic
* 若 reason 太籠統，後續人工 audit 的價值會下降
* 可在 P6 或 P7 之後接續處理

### 第三優先：暫緩 corpus expansion

不建議今天先擴 corpus。
若 bucket schema 還有 inconsistent，擴大 corpus 只會放大 schema 問題。

---

## 9. 下一輪可直接執行的 task prompt

```text
你是 Stock Prediction System 的 Senior Worker Agent。

任務名稱：
P6-HARDRESET — Bucket Schema Repair Diagnosis for Calibration Walkthrough Inconsistencies

日期：
2026-05-12

工作目錄：
/Users/kelvin/Kelvin-WorkSpace/Stock-Prediction-System

目前狀態：
P0-HARDRESET 已完成：
- real-price historical replay corpus
- p0hardreset_historical_replay_corpus.jsonl = 4500 lines
- mock-deterministic = 0

P1-HARDRESET 已完成：
- naive baseline corpus
- p1baseline_historical_replay_corpus.jsonl = 9900 lines
- 4 baseline types
- mock-deterministic = 0

P2-HARDRESET 已完成：
- prediction layer spot-check audit v0
- classification = P2_SPOTCHECK_LIMITED_BY_MISSING_SCORE_FIELDS

P3-HARDRESET 已完成：
- active scoring historical replay corpus
- p3active_scoring_historical_replay_corpus.jsonl = 4500 lines
- COMPLETE=3099 / PARTIAL=1401 / EMPTY=0
- usable ratio = 100%

P4-HARDRESET 已完成：
- Full bucket / score calibration audit
- classification = P4_FULL_CALIBRATION_AUDIT_COMPLETE
- walkthrough cases = 58 deterministic cases

P5-HARDRESET 已完成：
- Manual walkthrough calibration case review
- 58/58 explainability COMPLETE
- 53/58 score↔bucket CONSISTENT
- 5/58 score↔bucket INCONSISTENT
- 34/58 signal/reason CONSISTENT
- 24/58 signal/reason GENERIC
- 29/58 READY_FOR_NEXT_AUDIT
- tests = 68/68 PASS
- forbidden claims = 0
- final classification:
  P5_WALKTHROUGH_REVIEW_COMPLETE + P5_REQUIRES_BUCKET_SCHEMA_REPAIR
- commit:
  4c7cab7 P5-HARDRESET PART I: final report

本輪目標：
針對 P5 發現的 5 個 score↔bucket INCONSISTENT cases 做 bucket schema diagnosis。
本輪只做診斷、規格化與 repair proposal。
不得直接修改 production scoring formula。
不得宣稱績效。

核心問題：
P5 已證實 explainability 完整，但有 5 個 cases 出現 score 與 researchBucket 不一致。
本輪要找出不一致原因：
1. bucket mapping 問題
2. score threshold 問題
3. 中文/英文 bucket normalization 問題
4. factor aggregation 與 bucket assignment 不一致
5. active scoring snapshot capture 與原始 analyzer result 不一致

本輪不得：
- 寫 production DB
- 修改 SignalFusion / RuleBased / StrategyScreen scoring formula
- 修改 alphaScore / recommendationBucket 計算邏輯
- 修改 ActiveScoringSnapshotBuilder scoring 行為
- 修改 P0 / P1 / P3 / P4 corpus
- 修改 ManualReview* 模組
- 修改 simulation_snapshot_corpus.jsonl
- 呼叫外部 API / LLM
- 自動交易
- 輸出 buy/sell/recommendation/outperform/guaranteed
- 宣稱 ROI / win-rate / alpha / edge / profit
- 直接修模型或調權重

本輪可以：
- 新增 bucket schema diagnosis utilities
- 新增 inconsistency root-cause report
- 新增 bucket mapping / threshold contract artifact
- 新增 tests
- 產出 repair proposal
- 僅修改 diagnosis / observability artifacts

============================================================
PART A — Pre-flight Audit
============================================================

A.1 確認 artifacts 存在：
- outputs/online_validation/p5walkthrough_review.json
- outputs/online_validation/p5walkthrough_repair_backlog.json
- outputs/online_validation/p5walkthrough_final_report.md
- outputs/online_validation/p4calibration_walkthrough_cases.json
- outputs/online_validation/p4calibration_full_audit.json
- outputs/online_validation/p3active_scoring_historical_replay_corpus.jsonl
- outputs/online_validation/simulation_snapshot_corpus.jsonl

A.2 驗證 P5 結論：
- walkthrough cases = 58
- explainability COMPLETE = 58
- scoreBucketConsistency INCONSISTENT = 5
- repair backlog 包含 BUCKET_SCHEMA_REVIEW 或 equivalent P0 priority item
- forbidden claims = 0

A.3 Frozen corpus checks：
- simulation_snapshot_corpus.jsonl = 60 lines
- p0hardreset_historical_replay_corpus.jsonl = 4500 lines
- p1baseline_historical_replay_corpus.jsonl = 9900 lines
- p3active_scoring_historical_replay_corpus.jsonl = 4500 lines

輸出：
- outputs/online_validation/p6bucket_schema_preflight_audit.json
- outputs/online_validation/p6bucket_schema_preflight_audit.md

若 fail：
- outputs/online_validation/p6bucket_schema_escalation_report.md
- Final Classification = P6_BUCKET_SCHEMA_BLOCKED_BY_ARTIFACTS

============================================================
PART B — Bucket Schema Diagnosis Utils
============================================================

新增：
src/lib/onlineValidation/P6BucketSchemaDiagnosisUtils.ts

必須 export：
- extractInconsistentCases(reviewRows)
- normalizeBucketLabel(bucket)
- inferExpectedBucketFromScore(score, options)
- compareScoreToBucket(score, bucket, options)
- diagnoseBucketInconsistency(caseRow, options)
- summarizeBucketSchemaDiagnosis(diagnosisRows)
- buildBucketSchemaRepairProposal(summary)
- scanForbiddenClaims(text)

Bucket normalization 必須處理：
- Neutral
- Positive
- Negative
- Insufficient Data
- InsufficientData
- 資料不足
- 偏多 / positive-like labels
- 偏空 / negative-like labels
- unknown / missing

診斷分類：
1. BUCKET_MAPPING_MISMATCH
   - 同一語意 bucket 被不同 label 表示

2. SCORE_THRESHOLD_MISMATCH
   - score 落在某 threshold，但 bucket assignment 不一致

3. NORMALIZATION_GAP
   - 中英文或空白格式導致分類錯誤

4. SNAPSHOT_CAPTURE_MISMATCH
   - activeScoringSnapshot 與 top-level researchBucket / scoreSnapshot 不一致

5. FACTOR_AGGREGATION_AMBIGUOUS
   - factor/signal 與 score/bucket 方向不一致

6. UNKNOWN_REQUIRES_CODE_TRACE

規則：
- 不得用 realized return 決定 bucket 是否正確
- 不得說某 bucket 預測成功或失敗
- 只可檢查 score/bucket/schema 自洽性
- 不得提出投資建議

============================================================
PART C — Run Bucket Schema Diagnosis
============================================================

新增 script：
scripts/run-p6-bucket-schema-diagnosis.js

輸入：
- outputs/online_validation/p5walkthrough_review.json
- outputs/online_validation/p4calibration_walkthrough_cases.json
- outputs/online_validation/p3active_scoring_historical_replay_corpus.jsonl

輸出：
- outputs/online_validation/p6bucket_schema_diagnosis.json
- outputs/online_validation/p6bucket_schema_diagnosis.md

每個 inconsistent case 必須輸出：
- caseId
- symbol
- originalAsOfDate
- horizonDays
- score
- scoreSource
- researchBucket
- normalizedBucket
- activeScoringSnapshot bucket
- top-level bucket
- scoreSnapshot fields
- signal/factor summary
- reason summary
- diagnosisCategory
- evidence
- recommendedRepairType
- whyNoModelChangeNow

整體 summary 必須包含：
- inconsistentCaseCount
- diagnosisCategory distribution
- recommendedRepairType distribution
- examples by category
- whether schema repair is safe to implement next phase
- whether further code trace is needed

============================================================
PART D — Bucket Schema Contract Proposal
============================================================

新增 script：
scripts/build-p6-bucket-schema-contract-proposal.js

輸入：
- p6bucket_schema_diagnosis.json

輸出：
- outputs/online_validation/p6bucket_schema_contract_proposal.json
- outputs/online_validation/p6bucket_schema_contract_proposal.md

proposal 必須包含：
1. canonical bucket labels
2. allowed aliases
3. score threshold interpretation
4. normalization rules
5. snapshot consistency rules
6. test cases to add in next repair phase
7. explicit non-goals

Non-goals 必須包含：
- no scoring formula change
- no performance claim
- no investment recommendation
- no realized-return based tuning

============================================================
PART E — Tests
============================================================

新增測試：
src/lib/onlineValidation/__tests__/p6bucket_schema_diagnosis_utils.test.ts

至少涵蓋：
- normalizeBucketLabel handles English / Chinese / spacing variants
- inferExpectedBucketFromScore deterministic
- compareScoreToBucket handles boundary cases
- diagnoseBucketInconsistency identifies mapping mismatch
- diagnoseBucketInconsistency identifies normalization gap
- diagnoseBucketInconsistency identifies snapshot capture mismatch
- summarizeBucketSchemaDiagnosis deterministic
- repair proposal contains non-goals
- forbidden claim scanner catches ROI / alpha / edge / profit / outperform / buy / sell / guaranteed
- 不使用 Math.random
- 不修改 P0/P1/P3/P4 corpus

執行：
npx jest src/lib/onlineValidation/__tests__/p6bucket_schema_diagnosis_utils.test.ts --no-coverage
npx jest src/lib/onlineValidation/__tests__ --no-coverage
npx jest src/lib/data/__tests__ --no-coverage

============================================================
PART F — Forbidden Claims Scan
============================================================

掃描：
grep -RniE "ROI|win-rate|win rate|alpha|edge|profit|outperform|beat|buy|sell|guaranteed|investment recommendation" \
  outputs/online_validation/p6bucket_schema_* src/lib/onlineValidation/P6BucketSchemaDiagnosisUtils.ts scripts/run-p6-bucket-schema-diagnosis.js scripts/build-p6-bucket-schema-contract-proposal.js || true

允許：
- disclaimer context
- forbidden claim scanner test data
- 欄位名稱 alphaScore

若有非 disclaimer 語境，必須修正。

============================================================
PART G — Artifact Validation
============================================================

JSON parse：
for f in outputs/online_validation/p6bucket_schema_*.json; do
  node -e "JSON.parse(require('fs').readFileSync('$f','utf8')); console.log('$f OK')"
done

Frozen line count check：
node -e "
const fs = require('fs');
const expected = {
  'outputs/online_validation/simulation_snapshot_corpus.jsonl': 60,
  'outputs/online_validation/p0hardreset_historical_replay_corpus.jsonl': 4500,
  'outputs/online_validation/p1baseline_historical_replay_corpus.jsonl': 9900,
  'outputs/online_validation/p3active_scoring_historical_replay_corpus.jsonl': 4500
};
for (const [path, count] of Object.entries(expected)) {
  const lines = fs.readFileSync(path, 'utf8').trim().split('\n').length;
  console.log(path, lines);
  if (lines !== count) throw new Error(path + ' changed: ' + lines);
}
console.log('all frozen line counts OK');
"

P6 artifact structure check：
node -e "
const fs = require('fs');
const diagnosis = JSON.parse(fs.readFileSync('outputs/online_validation/p6bucket_schema_diagnosis.json','utf8'));
const proposal = JSON.parse(fs.readFileSync('outputs/online_validation/p6bucket_schema_contract_proposal.json','utf8'));
if (!diagnosis.summary) throw new Error('missing diagnosis.summary');
if (!Array.isArray(diagnosis.cases)) throw new Error('missing diagnosis.cases');
if (!proposal.canonicalBucketLabels) throw new Error('missing canonicalBucketLabels');
if (!proposal.nonGoals) throw new Error('missing nonGoals');
console.log('P6 diagnosis cases:', diagnosis.cases.length);
console.log('P6 canonical labels:', proposal.canonicalBucketLabels.length);
"

============================================================
PART H — Commit
============================================================

若全部 PASS，commit：

P6-HARDRESET: Bucket schema inconsistency diagnosis

- New: bucket schema diagnosis utilities
- New: diagnosis of 5 score/bucket inconsistent cases
- New: canonical bucket schema contract proposal
- Frozen: P0/P1/P3/P4/frozen corpus unchanged
- Frozen: ManualReview* modules unchanged
- No ROI / alpha / edge / win-rate claims

============================================================
PART I — Final Report
============================================================

新增：
outputs/online_validation/p6bucket_schema_final_report.md

格式：

P6-HARDRESET Final Report

1. 本輪目標
2. Pre-flight audit 結論
3. P5 inconsistent cases 使用情況
4. Bucket schema diagnosis 結果
   - diagnosisCategory distribution
   - recommendedRepairType distribution
   - representative examples
5. Bucket schema contract proposal
6. 是否建議下一輪實作 repair
7. 不做模型調整的原因
8. 修改 / 新增檔案清單
9. 測試結果 / regression 結果
10. Forbidden claims scan 結果
11. 凍結驗證
    - simulation_snapshot_corpus.jsonl 60 lines unchanged
    - P0 corpus 4500 lines unchanged
    - P1 corpus 9900 lines unchanged
    - P3 corpus 4500 lines unchanged
    - ManualReview* unchanged
12. 對 CEO 兩大主軸的貢獻
    - 主軸 A：直接貢獻，修補 prediction bucket schema 的可解釋性與一致性
    - 主軸 B：間接貢獻，提高 calibration / simulation audit 的 bucket 維度可信度
13. 風險與不確定點
14. 下一輪建議
    - 若 repair safe：P7 Bucket Schema Repair Implementation
    - 若 requires code trace：P7 Bucket Assignment Code Trace
    - 若 mostly signal issue：P7 Signal / Reason Snapshot Quality Repair
15. Final Classification

Final Classification 六選一：
1. P6_BUCKET_SCHEMA_DIAGNOSIS_COMPLETE
2. P6_READY_FOR_BUCKET_SCHEMA_REPAIR
3. P6_REQUIRES_BUCKET_ASSIGNMENT_CODE_TRACE
4. P6_REQUIRES_SIGNAL_REASON_REPAIR
5. P6_BUCKET_SCHEMA_BLOCKED_BY_ARTIFACTS
6. P6_NO_BUCKET_SCHEMA_REPAIR_NEEDED
```

---

## CTO Agent 10 行內摘要

1. P5-HARDRESET 已完成，commit `4c7cab7`。
2. 本輪完成 58 個 P4 walkthrough cases 的 manual-style engineering review。
3. 58/58 explainability COMPLETE，代表 active scoring snapshot 可讀性已達基本門檻。
4. 53/58 score↔bucket CONSISTENT，另有 5 筆 INCONSISTENT，列為 P0 repair target。
5. 34/58 signal/reason CONSISTENT，24/58 GENERIC，列為 P1 repair target。
6. 68/68 tests PASS，forbidden claims = 0。
7. P0/P1/P3/frozen corpus 全部維持 unchanged。
8. 本輪沒有模型調整、沒有 scoring formula 修改、沒有績效宣稱。
9. 目前結論：`P5_WALKTHROUGH_REVIEW_COMPLETE + P5_REQUIRES_BUCKET_SCHEMA_REPAIR`。
10. 今日建議優先執行 P6：Bucket Schema Repair Diagnosis，先診斷 5 個 inconsistent cases。
